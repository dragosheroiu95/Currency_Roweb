import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    connectionStatus: {
        position: 'absolute',
        bottom: 0,
        width: '100%',
        backgroundColor: 'red',
        alignItems: 'center',
    },
    connectionText: {
        color: 'white',
        fontSize: 8,
        fontWeight: 'bold',
    },
})